{
  "name": "vch-sampling-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VCH soil sampling — sampler PWA, plan-point ingest tool, sync spine",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "uuidv7": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
